<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.FilterSelectorRadioButton">
        <item name="android:layout_width">@dimen/top_rank_filter_item_width</item>
        <item name="android:layout_height">@dimen/top_rank_filter_item_height</item>
        <item name="android:background">@drawable/filter_selector_radio_button_background</item>
        <item name="android:textColor">@color/filter_selector_radio_button_font</item>
        <item name="android:button">@null</item>
    </style>

</resources>
